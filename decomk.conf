# Shared workspace configuration
# DEFAULT applies to all repos. Repo-specific keys add tools on top.
# Macros (like FPGA) group related targets for easy reuse.

DEFAULT: TOOLS GO PYTHON

FPGA: OSS I2C COCOTB

fpga-workbench: DEFAULT FPGA
